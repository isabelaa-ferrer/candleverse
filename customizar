<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CandleVerse - Sua Vela Personalizada</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
        }

        .header h1 {
            color: #ff6ec7;
            font-size: 32px;
            margin-bottom: 10px;
        }

        .header p {
            color: #666;
            font-size: 16px;
        }

        .candle-preview {
            background: linear-gradient(180deg, #f8f9fa 0%, #e9ecef 100%);
            border-radius: 15px;
            padding: 40px;
            margin-bottom: 30px;
            text-align: center;
            min-height: 300px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        .candle-mockup {
            font-size: 100px;
            margin-bottom: 20px;
            filter: drop-shadow(0 10px 20px rgba(0, 0, 0, 0.2));
        }

        .candle-details {
            background: white;
            border-radius: 10px;
            padding: 20px;
            margin-top: 20px;
            width: 100%;
            max-width: 400px;
        }

        .detail-item {
            display: flex;
            justify-content: space-between;
            padding: 10px 0;
            border-bottom: 1px solid #e0e0e0;
        }

        .detail-item:last-child {
            border-bottom: none;
        }

        .detail-label {
            font-weight: 600;
            color: #333;
        }

        .detail-value {
            color: #666;
        }

        .actions {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-bottom: 20px;
        }

        .btn {
            padding: 15px 25px;
            border: none;
            border-radius: 10px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }

        .btn:active {
            transform: translateY(0);
        }

        .btn-primary {
            background: #ff6ec7;
            color: white;
        }

        .btn-primary:hover {
            background: #e55bb0;
        }

        .btn-secondary {
            background: #b283ff;
            color: white;
        }

        .btn-secondary:hover {
            background: #9c6fe6;
        }

        .btn-tertiary {
            background: #ffa94d;
            color: white;
        }

        .btn-tertiary:hover {
            background: #ff8c1a;
        }

        .btn-outline {
            background: white;
            color: #ff6ec7;
            border: 2px solid #ff6ec7;
        }

        .btn-outline:hover {
            background: #ff6ec7;
            color: white;
        }

        .message {
            padding: 15px;
            border-radius: 10px;
            margin-bottom: 20px;
            text-align: center;
            display: none;
        }

        .message.show {
            display: block;
        }

        .message.success {
            background: #4caf50;
            color: white;
        }

        .message.error {
            background: #ff4444;
            color: white;
        }

        .share-options {
            display: none;
            background: #f8f9fa;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 20px;
        }

        .share-options.show {
            display: block;
        }

        .share-options h3 {
            color: #333;
            margin-bottom: 15px;
            text-align: center;
        }

        .share-buttons {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
            justify-content: center;
        }

        .logout-btn {
            position: absolute;
            top: 20px;
            right: 20px;
            background: none;
            border: none;
            color: #666;
            cursor: pointer;
            font-size: 14px;
            padding: 8px 15px;
            border-radius: 8px;
            transition: all 0.3s;
        }

        .logout-btn:hover {
            background: rgba(255, 255, 255, 0.2);
            color: #333;
        }

        @media (max-width: 768px) {
            .container {
                padding: 30px 20px;
            }

            .header h1 {
                font-size: 24px;
            }

            .candle-mockup {
                font-size: 80px;
            }

            .actions {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <button class="logout-btn" onclick="logout()">🚪 Sair</button>

    <div class="container">
        <div class="header">
            <h1>🕯️ Sua Vela Personalizada</h1>
            <p>Veja o resultado final da sua criação</p>
        </div>

        <div class="message" id="message"></div>

        <div class="candle-preview">
            <div class="candle-mockup" id="candleMockup">🕯️</div>
            
            <div class="candle-details" id="candleDetails">
                <div class="detail-item">
                    <span class="detail-label">Cor:</span>
                    <span class="detail-value" id="colorValue">-</span>
                </div>
                <div class="detail-item">
                    <span class="detail-label">Aroma:</span>
                    <span class="detail-value" id="aromaValue">-</span>
                </div>
                <div class="detail-item">
                    <span class="detail-label">Recipiente:</span>
                    <span class="detail-value" id="containerValue">-</span>
                </div>
                <div class="detail-item">
                    <span class="detail-label">Tamanho:</span>
                    <span class="detail-value" id="sizeValue">-</span>
                </div>
            </div>
        </div>

        <div class="share-options" id="shareOptions">
            <h3>Compartilhar Design</h3>
            <div class="share-buttons">
                <button class="btn btn-secondary" onclick="shareWhatsApp()">
                    📱 WhatsApp
                </button>
                <button class="btn btn-tertiary" onclick="shareEmail()">
                    📧 E-mail
                </button>
            </div>
        </div>

        <div class="actions">
            <button class="btn btn-primary" onclick="saveDesign()">
                💾 Salvar Design
            </button>
            <button class="btn btn-secondary" onclick="toggleShareOptions()">
                📤 Compartilhar
            </button>
            <button class="btn btn-tertiary" onclick="downloadImage()">
                📥 Baixar Imagem
            </button>
            <button class="btn btn-outline" onclick="createNew()">
                ➕ Criar Nova Vela
            </button>
        </div>
    </div>

    <script type="module">
        import { initializeApp } from 'https://www.gstatic.com/firebasejs/10.7.1/firebase-app.js';
        import { getAuth, onAuthStateChanged, signOut } from 'https://www.gstatic.com/firebasejs/10.7.1/firebase-auth.js';
        import { getFirestore, collection, addDoc, serverTimestamp } from 'https://www.gstatic.com/firebasejs/10.7.1/firebase-firestore.js';

        // Configuração do Firebase (SUBSTITUA com suas credenciais)
        const firebaseConfig = {
            apiKey: "SUA_API_KEY_AQUI",
            authDomain: "seu-projeto.firebaseapp.com",
            projectId: "seu-projeto-id",
            storageBucket: "seu-projeto.appspot.com",
            messagingSenderId: "123456789",
            appId: "seu-app-id"
        };

        const app = initializeApp(firebaseConfig);
        const auth = getAuth(app);
        const db = getFirestore(app);

        let currentUser = null;
        let candleDesign = null;

        // Verificar autenticação
        onAuthStateChanged(auth, (user) => {
            if (user) {
                currentUser = user;
                loadCandleDesign();
            } else {
                window.location.href = '/login';
            }
        });

        // Carregar design da vela
        function loadCandleDesign() {
            // Tentar recuperar do sessionStorage
            const savedDesign = sessionStorage.getItem('candleDesign');
            
            if (savedDesign) {
                candleDesign = JSON.parse(savedDesign);
            } else {
                // Design de exemplo para demonstração
                candleDesign = {
                    cor: 'Rosa Claro',
                    aroma: 'Lavanda',
                    recipiente: 'Vidro Transparente',
                    tamanho: 'Médio (200g)'
                };
            }

            displayCandleDesign();
        }

        // Exibir design
        function displayCandleDesign() {
            document.getElementById('colorValue').textContent = candleDesign.cor;
            document.getElementById('aromaValue').textContent = candleDesign.aroma;
            document.getElementById('containerValue').textContent = candleDesign.recipiente;
            document.getElementById('sizeValue').textContent = candleDesign.tamanho;

            // Atualizar emoji baseado nas escolhas
            const mockup = document.getElementById('candleMockup');
            const colorEmojis = {
                'Rosa': '🌸',
                'Lilás': '💜',
                'Laranja': '🧡',
                'Amarelo': '💛',
                'Azul': '💙'
            };

            // Encontrar cor correspondente
            for (const [key, emoji] of Object.entries(colorEmojis)) {
                if (candleDesign.cor.includes(key)) {
                    mockup.textContent = `${emoji}🕯️`;
                    break;
                }
            }
        }

        // Salvar design no Firestore
        window.saveDesign = async function() {
            if (!currentUser || !candleDesign) {
                showMessage('Erro ao salvar design', 'error');
                return;
            }

            try {
                const designData = {
                    userId: currentUser.uid,
                    userEmail: currentUser.email,
                    cor: candleDesign.cor,
                    aroma: candleDesign.aroma,
                    recipiente: candleDesign.recipiente,
                    tamanho: candleDesign.tamanho,
                    createdAt: serverTimestamp()
                };

                const docRef = await addDoc(collection(db, 'designs'), designData);
                console.log('Design salvo com ID:', docRef.id);
                
                showMessage('✅ Design salvo com sucesso!', 'success');
            } catch (error) {
                console.error('Erro ao salvar design:', error);
                showMessage('❌ Erro ao salvar design. Tente novamente.', 
